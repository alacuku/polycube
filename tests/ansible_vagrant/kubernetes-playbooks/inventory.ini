#make sure that each host is present in /etc/hosts with its ip associated to the host name
#otherwise just substitute the names with the IPs of the machines that are used for the deployment

[k8s_master_nodes]
k8s-master 

[k8s_worker_nodes]
node-1
node-2

## All nodes
[all_nodes:children]
k8s_master_nodes
k8s_worker_nodes